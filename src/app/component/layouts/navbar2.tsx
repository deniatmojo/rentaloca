import Image from "next/image";
const navbar2 = () => {
  return (
    <header className="container bg-white">
      <nav
        className="font-Inter font-semibold mr-0 ml-0 flex p-4 border-b border-gray-200"
        aria-label="Global"
      >
        {/* Logo */}
      </nav>
    </header>
  );
};

export default navbar2;
